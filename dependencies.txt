Fonts:

    Victor Mono && VictorMono Nerd Font
    [both are included in .fonts directory;
     just copy its contents into
     /usr/share/fonts or ~/.fonts;
     I'll try to update them on time]

WMs:

    -> i3 (i3-gaps i3status i3lock rofi)
    [I haven't been using i3 for a while for now
     so the config is pretty outdated
     soon maybe I will update it with polybar
     or some other fancy bar]

    -- or --

    -> dwm (dwm dmenu slstatus)
    [dwm, dmenu and slstatus are better
     to be downloaded from suckless.org]

Terminals:

    alacritty
    [I went back to alacritty as it
     works fine with X11 again]

Console Applications:

    nvim (neovim)
    [modern vim replacement]

    exa
    [modern ls replacement with icons
     note: icons are shown properly
     only with nerd fonts]

    ranger
    [simple terminal file manager with vim keybindings]

    zsh
    [better shell; use with ohmyzsh framework]

Utils:

    pcmanfm
    -- or --
    thunar
    [GUI file manager, if you need one]

    scrot
    [simple screenshot utility]

    sxiv
    [simple X image viewer]

    -> zathura (zathura zathura-pdf-mupdf zathura-djvu)
    [simple pdf and djvu reader with vim keybindings]

    picom
    [compton fork; more powerful screen compositor
     than xcompmgr; for i3 setup]

    xcompmgr
    [simple X compositor; for dwm setup]

    xdg-user-dirs
    [Desktop, Downloads, etc folders support]

Theming:

    qt5ct && kvantum
    [qui-based utility to set up qt themes in window managers
     you should append 'export QT_QPA_PLATFORMTHEME="qt5ct"'
     to /etc/profile]
    [you only need it if you don't use KDE, LXQT
     or other QT-based DE]

    lxappearance
    [GUI-based utility for setting up gtk themes and fonts]
    [you only need it if you don't have theme configurer
     in your desktop environment]

    arc-gtk-theme
    papirus-icon-theme

    [cursors are included in ./themes/.icons folder]
    [kvantum (qt) themes are included in ./themes/kvantum-themes folder]

Other:

    pfetch (https://github.com/dylanaraps/pfetch.git)
    [simple alternative to screenfetch or neofetch]

